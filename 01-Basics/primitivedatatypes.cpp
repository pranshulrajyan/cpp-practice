#include <iostream>
using namespace std;

int main(){
    int age = 25;
    char grade = 'A';
    float score = 93.66f;
    cout << " the age of ram is " << age << "\n the grade secured by him is " << grade << "\n this corresponds to the score of " << score << endl;
    cout << (age <= 20) << endl; // uses bool
    cout << (age >=20) << endl; //uses bool 
    return 0;
}